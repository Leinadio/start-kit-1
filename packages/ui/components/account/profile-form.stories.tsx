import type { Meta, StoryObj } from "@storybook/react-vite"
import { ProfileForm } from "@/components/account/profile-form"

const meta: Meta<typeof ProfileForm> = { title: "Account/ProfileForm", component: ProfileForm }
export default meta
export const Defaut: StoryObj<typeof ProfileForm> = {}
